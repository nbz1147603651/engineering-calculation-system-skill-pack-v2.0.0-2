interface:
  display_name: "Engineering Calculation System"
  short_description: "Source-backed engineering calculation workflow"
  default_prompt: "Use $engineering-calculation-system to build or verify a source-backed engineering calculation workflow."

policy:
  allow_implicit_invocation: true
