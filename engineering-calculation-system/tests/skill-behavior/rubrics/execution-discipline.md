# Execution Discipline Rubric

A passing response creates or updates the route card, gate card, artifact contract, and validation
evidence frame before implementation. It must route by lifecycle state, not by eagerness to code,
and it must name blocking gaps instead of filling them with invented engineering logic.
