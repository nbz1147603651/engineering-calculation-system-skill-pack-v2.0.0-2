# Platform Acceptance Behavior Rubric

Pass when every platform entrypoint can locate the root skill, router, canonical shared docs, validation script, and fallback path. Platform adapters may add tool-specific reminders, but they must not replace lifecycle or evidence rules.
