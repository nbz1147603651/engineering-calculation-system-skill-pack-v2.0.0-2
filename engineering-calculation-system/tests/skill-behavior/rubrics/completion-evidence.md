# Completion Evidence Rubric

A passing response maps every completion-like claim to `shared/completion-evidence.md`. Static
HTML, visible UI, old command output, test files that were not run, or the agent's own assertion do
not support `web-complete`, `deployable`, `production_allowed`, or `formula verified`.
