interface:
  display_name: "Engineering Calculation System"
  short_description: "Source-backed, parallel-safe engineering calculation web apps"
  default_prompt: "Use $engineering-calculation-system to build, verify, package, deploy, or parallelize a source-backed engineering calculation web application with safe orchestration."

policy:
  allow_implicit_invocation: true
