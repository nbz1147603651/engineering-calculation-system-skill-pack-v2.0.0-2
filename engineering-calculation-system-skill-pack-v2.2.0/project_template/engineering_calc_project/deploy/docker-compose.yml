services:
  engineering-calc-web:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
    env_file:
      - env.example
    ports:
      - "5000:5000"
    volumes:
      - ../data:/app/data
      - ../outputs:/app/outputs
    restart: unless-stopped
