interface:
  display_name: "Engineering Calculation System"
  short_description: "Source-backed, deployable engineering calculation web apps"
  default_prompt: "Use $engineering-calculation-system to build, verify, package, or deploy a source-backed engineering calculation web application."

policy:
  allow_implicit_invocation: true
